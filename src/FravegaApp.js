import { Carousel, Header, PayInfo } from './layout';

const FravegaApp = () => {
   return (
      <>
         <Header />
         <Carousel />
         <PayInfo />
      </>
   );
};

export default FravegaApp;
